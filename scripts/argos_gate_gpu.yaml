# Arquivo: data.yaml para treino Local (WSL)
path: /mnt/c/ArgosCam/dataset_yolo  # Raiz do dataset
train: images/train                 # Caminho relativo à raiz acima
val: images/val                     # Caminho relativo à raiz acima

# Definição das Classes do Argos Gate
nc: 6
names: ['caido', 'deitado', 'em_pe', 'caindo', 'sentado', 'agachado']